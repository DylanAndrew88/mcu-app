
  <!DOCTYPE html>
  <html>
    <head>
      <meta charset="utf-8">
      <title>{{title}} - Home</title>
    </head>
    <body>
      <h1>{{title}}</h1>
      <ol>
        {{#movies}}
        <li>{{title}}</li>
        <img src="/images/{{poster}}" class="poster">
        {{/movies}}
      </ol>
    </body>
  </html>
